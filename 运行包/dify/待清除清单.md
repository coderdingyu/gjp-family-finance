# 导入旧链路待清除清单

工作流已改成：本机抽得出就本机；抽不出把**原文件**交给「管家婆账单导入」（文档提取 → 空则 PaddleOCR-VL → LLM）。  
下面是旧链路残留。**现在先记着，不要提前删。** 用户说「按清单清除」时再整批做。

仓库：`管家婆-家庭收支管理系统-完整交付包`，分支 `dingyu`。  
清完后若动了 Java / 前端，需重新 `npm run build` + `mvn -DskipTests package`，覆盖 `运行包/gjp-server-1.0.0.jar` 再重启。不要 commit，除非用户明确要求。

---

## A. 确定已死，整批可删

### A1. Dify 小工作流「管家婆读文件」

以前给经典 Agent 当读文件工具，主流程已内联文档提取器。

- [ ] 删仓库文件 `运行包/dify/管家婆读文件.yml`
- [ ] 在 Dify 工作室删应用 **管家婆读文件**（App ID `32e60d3c-e581-45e6-ad3c-adf4d1abf84b`）
- [ ] 不要动主应用 **管家婆账单导入**（`c245420c-5701-463b-87f7-10922c4f80ec`），不要新建应用换 Key

### A2. 文本切块（每 50 行送模型）

线上 `ImportService` 只调用 `ExcelTextExtractor.tableText()`。切块不再送 Dify。

- [ ] `源码/server/src/main/java/com/gjp/imp/ExcelTextExtractor.java`
  - 删 `CHUNK_ROWS`、`chunks()`、`textChunks()`、`chunkLines()`
  - 保留 `tableText()`、`slim()`、`dropPreamble()` 及读表私有方法
- [ ] `源码/server/src/test/java/com/gjp/imp/BillTextParserTest.java`
  - `wechatPreambleIsNotHeader`：去掉对 `chunkLines` 的断言，只测 slim / 去单号
  - 删整个测试 `textChunksSplitByFiftyRows`
- [ ] `运行包/测试样例/怪表-300行-ground-truth.json` 的 `expected_path`  
  现应是：本机 `BillTextParser` 抽出 300 笔，不走 Dify

### A3. `BillAgent.parseText()`

没有任何调用方。切块送通义 / 再回退本机规则的入口。

- [ ] 从 `源码/server/src/main/java/com/gjp/dify/BillAgent.java` 删 `parseText(...)`
- [ ] 同步改类注释：本机认不出只走 `parseFile` → Dify 原文件

---

## B. 通义本机直连（建议一并清）

本机没有 `DASHSCOPE_API_KEY`。`parseFile` 配了 Dify 就不会走通义。已约定不恢复本机 OCR / 通义自建 tools。

### B1. Java

- [ ] 删 `源码/server/src/main/java/com/gjp/dify/QwenClient.java`
- [ ] 删 `源码/server/src/main/java/com/gjp/dify/QwenProperties.java`
- [ ] 删 `源码/server/src/main/java/com/gjp/dify/ImagePrep.java`
- [ ] `BillAgent`：去掉 `QwenClient` 依赖；`configured()` / `mode()` 只看 Dify；`parseFile` 未配 Dify 时直接报「请先配置 Dify API Key」，不再回退视觉
- [ ] `ImportConfig`：`@EnableConfigurationProperties` 去掉 `QwenProperties.class`
- [ ] `源码/server/src/main/resources/application.yml`：删整段 `gjp.qwen`
- [ ] `BillTextParserTest`：删测试 `shrinkLargeImageForVision` 及对 `ImagePrep` 的 import

### B2. 环境变量模板

- [ ] `运行包/dify/env.example`：删 `DASHSCOPE_API_KEY=`
- [ ] 本机启动脚本 / `tmp/dify-gjp.env`：若有 `DASHSCOPE_*` / `QWEN_*` 可去掉（不要把该 env 提交 git）

### B3. 过期说明（清通义时一起改，避免文档仍教人配第二套 Key）

- [ ] `运行包/dify/导入说明.md`
  - 删第 5.1 节「通义直连」及所有 `DASHSCOPE_API_KEY` / `QWEN_*` 示例
  - 启动示例只保留 `DIFY_BASE_URL` / `DIFY_API_KEY` / `DIFY_MODE=workflow`
  - 删「表格和小票优先直连通义」
  - `text_content` 可注明：后端现在传空，工作流以附件文字为准（变量可留着兼容）
- [ ] `运行包/dify/云主机迁移.md`
  - 删「表格路径若已导出 DASHSCOPE 会直连通义」
  - 编排验收改成：文档提取 →（空则 PaddleOCR）→ LLM，**不是**智能体节点挂「读账单文件」
  - FAQ「读文件工具没有字」改成查 `FILES_URL` / `INTERNAL_FILES_URL` 和 PaddleOCR 授权
- [ ] `源码/docs/接口清单.md`：去掉「表格优先直连通义」「认不出的表才交给通义文本」
- [ ] `源码/web/src/views/import/ImportView.vue` 页头说明：改成「文档提取或 PaddleOCR」，不要写「市场读文件工具」

---

## C. 可选，不是必须

这些还能跑，只是当前部署用不到。整批清 A+B 时再看要不要顺手收。

- [ ] `DifyClient.runChat` + `SYSTEM_PROMPT` + `buildQuery`：仅 `DIFY_MODE=chat` 会走。现默认且实测都是 `workflow`。若确定永不接对话型应用，可删，`parse` 只留 `runWorkflow`
- [ ] 工作流开始节点的 `text_content`：Java 仍传空字符串。可留着兼容，**不要**为清它去改已发布主工作流
- [ ] `DifyClient` 里 `textContent` 参数：A/B 清完后调用处已是 `null`，可简化签名，但要同步 DSL 仍收该变量

---

## D. 不要删

| 保留 | 原因 |
| --- | --- |
| `运行包/dify/管家婆账单导入.yml` 及已发布主应用 | 现行工作流 |
| Dify 插件 PaddleOCR、通义供应商（工作室里给 LLM 节点用） | 图片 / 扫描件和 `qwen3.8-max` 还靠它们 |
| `DifyClient` 上传原文件 + `runWorkflow` | 本机失败后的唯一模型入口 |
| `BillTextParser`、`WeChatPdfParser`、`AlipayPdfParser`、`ExcelTextExtractor.tableText` | 本机能抽的仍本机抽 |
| `ImagePrep` 以外的导入线程池 `importExecutor` / `llmExecutor` | 导入排队仍在用 |
| `FILES_URL` / `INTERNAL_FILES_URL`（Dify `docker/.env`） | 图片走 PaddleOCR 必需 |
| 历史失败导入任务（如任务 76） | 不用为清代码去删用户记录 |
| 通义节点 `temperature ≥ 0.6` | 供应商限制，不是旧功能 |

---

## 建议执行顺序

1. 先 A（读文件 DSL/应用、切块、`parseText`）
2. 再 B（通义 Java + 文档口径）
3. 编译、单测、换 jar、重启 8080
4. 用已有样例冒烟：规整 CSV（本机）、认不出的大写金额 CSV（Dify）、一张小票图（PaddleOCR）；**不要点待确认入库**
5. C 有空再收
6. 本清单全部勾完后可删本文件

清的时候不要恢复本机 OCR、不要把账单解析改回经典 Agent 挂 file 工具、不要整库跑 `schema.sql`。
