-- V1 baseline placeholder.
-- Fresh installs: run 运行包/schema.sql (or 源码/server/src/main/resources/db/schema.sql) first.
-- Live / already-populated DBs: Flyway baseline-on-migrate=true, baseline-version=1
-- marks this version as already applied and does NOT execute it.
-- This file is intentionally a no-op. Do not CREATE/DROP anything here.
SELECT 1;
