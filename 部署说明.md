# 《管家婆 — 家庭收支管理系统》完整环境包

本包含源码、可直接运行的 jar、以及 JDK/Maven 工具链，用于在新机器上快速跑起来做演示或继续开发。

## 一、包内结构

```
管家婆-完整环境包/
├── 部署说明.md                   本文件
├── 第n组-项目计划书.docx
├── 第n组-源码.zip                课程要求提交的源码包，可直接作为附件交（内容 = 下面的 源码/）
├── 源码/                         完整工程源码（含 README.md、docs/、一键脚本）
│   ├── server/                   后端 Java 源码
│   ├── web/                      前端 Vue 源码（不含 node_modules）
│   ├── docs/                     接口清单 + 11 张界面截图
│   ├── 打包.sh / 启动.sh / 停止.sh
│   └── README.md                 详细的目录结构与设计说明
├── 运行包/
│   ├── gjp-server-1.0.0.jar      可执行 jar，已内置前端页面
│   ├── schema.sql                建库建表
│   └── data.sql                  演示数据（962 笔流水）
└── 环境/
    ├── 安装环境.sh               一键把 JDK/Maven 解压到 ~/tools
    ├── jdk-17.0.20.1-macos-x64.tar.gz   本地交付包有；GitHub 因 100MB 限制不含，安装脚本会自动下载
    ├── apache-maven-3.9.16-bin.tar.gz
    ├── web-node_modules.tar.gz   前端依赖，供离线开发
    └── env.sh                    JAVA_HOME / PATH 设置
```

## 二、最快路径：只演示，不改代码（3 步）

前提：机器上已有 MySQL 8.0+ 并已启动，root 可空密码登录。
若 root 有密码，改 `源码/server/src/main/resources/application.yml` 里的 `password`，
然后按第四节重新打包。

```bash
# 1. 装 JDK（Node 不需要，前端页面已在 jar 里）
cd 环境 && ./安装环境.sh && source ~/tools/env.sh && cd ..

# 2. 建库 + 导入演示数据
mysql -u root < 运行包/schema.sql
mysql -u root < 运行包/data.sql

# 3. 启动
java -jar 运行包/gjp-server-1.0.0.jar
```

浏览器打开 <http://localhost:8080>，用 **zhangwei / 123456** 登录。

> 也可以直接用 `源码/启动.sh`：它会自动启 MySQL、首次运行自动建库灌数据、
> 起后端并打开浏览器。但它读的是 `源码/server/target/` 下的 jar，
> 需要先按第四节打一次包，或把 `运行包/gjp-server-1.0.0.jar` 拷到那个目录。

## 三、开发模式：要改代码

```bash
# 1. 装 JDK + Maven
cd 环境 && ./安装环境.sh && source ~/tools/env.sh && cd ..

# 2. 前端依赖（联网就用 npm install；不想联网就解压本包里的 node_modules）
cd 源码/web
tar -xzf ../../环境/web-node_modules.tar.gz      # 或者： npm install
cd ../..

# 3. 建库灌数据（同上）
mysql -u root < 运行包/schema.sql
mysql -u root < 运行包/data.sql

# 4. 起后端（8080）
cd 源码/server && mvn spring-boot:run

# 5. 另开一个终端起前端（5173，带热更新）
cd 源码/web && npm run dev
```

开发时访问 <http://localhost:5173>（Vite 会把 `/api` 代理到 8080）。

## 四、重新打包成单个 jar

```bash
source ~/tools/env.sh
cd 源码 && ./打包.sh
```

脚本依次做：编译前端 → 把 `web/dist` 复制进 jar 的 `static/` 目录 → 打出可执行 jar。
产物在 `源码/server/target/gjp-server-1.0.0.jar`。

> **首次执行 `打包.sh` 需要联网**：Maven 要下载 Spring Boot 等依赖到 `~/.m2`。
> 下载过一次后就可以离线构建。若现场不便联网，直接用 `运行包/gjp-server-1.0.0.jar`，
> 那个 jar 已经把依赖和前端页面都打进去了，不需要 Maven 也不需要 Node。

单 jar 能同时提供前端页面和后端接口，靠两处配合：

- `server/pom.xml` 里的 `maven-resources-plugin` 在 `prepare-package` 阶段复制 `web/dist`
- `com.gjp.common.SpaController` 把前端的 history 模式路由（`/home`、`/record` 等）
  转发到 `index.html`，否则直接访问或刷新这些地址会 404。
  **新增前端页面时，记得在 SpaController 的路径列表里补一条。**

## 五、本包未包含的东西

| 组件  | 为什么没打包                                     | 怎么装                        |
| ----- | ------------------------------------------------ | ----------------------------- |
| MySQL | 服务端体积大、数据目录与系统强绑定，跨机拷贝易出问题 | `brew install mysql`          |
| Node  | 只演示不改前端时用不到                            | `brew install node`           |

## 六、平台说明

`环境/` 里的 JDK 是 **macOS x86_64（Intel）** 版本。换平台需要自行下载对应版本，
Maven 和源码本身与平台无关：

- Apple Silicon (M 系列)：<https://api.adoptium.net/v3/binary/latest/17/ga/mac/aarch64/jdk/hotspot/normal/eclipse>
- Windows x64：<https://api.adoptium.net/v3/binary/latest/17/ga/windows/x64/jdk/hotspot/normal/eclipse>
- Linux x64：<https://api.adoptium.net/v3/binary/latest/17/ga/linux/x64/jdk/hotspot/normal/eclipse>

Maven 官方下载：<https://dlcdn.apache.org/maven/maven-3/>
（注意该站点只保留最新补丁版，写死版本号可能 404，需先列目录确认当前版本。）

## 七、常见问题

**中文乱码**：`schema.sql` 与 `data.sql` 开头都有 `SET NAMES utf8mb4;`。
若换成 Navicat 等工具导入，确认连接字符集也是 utf8mb4，否则中文会被按 latin1 双重编码。

**`java`/`mvn` 找不到**：JDK 和 Maven 装在 `~/tools`，不在系统 PATH 里。
每个新终端先 `source ~/tools/env.sh`。

**8080 被占用**：`源码/停止.sh` 可停掉已在跑的后端。

**前端页面 404 或白屏**：说明 jar 里没有前端产物，按第四节重新打包一次。
