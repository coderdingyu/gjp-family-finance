# 接口清单

约定：
- 统一前缀 `/api`，前端通过 Vite 代理到 `http://localhost:8080`
- 统一返回体 `{ code, msg, data }`，`code = 0` 表示成功，`401` 表示未登录，`400` 表示业务/参数错误
- 登录态基于 Session（Cookie `JSESSIONID`），除登录注册外所有接口都经过 `LoginInterceptor`
- `familyId` 由服务端从 Session 取，**不接受前端传参**
- 统计类接口的区间参数统一支持三种写法，优先级从高到低：
  `startDate + endDate` > `year + month` > 只传 `year`；都不传默认当年

## 一、登录注册 `/api/auth`（甲）

| 方法 | 路径        | 参数                                        | 说明                                       |
| ---- | ----------- | ------------------------------------------- | ------------------------------------------ |
| POST | `/register` | `username, password, realName, familyName`  | 建家庭 + 建账号 + 建默认成员 + 初始化预置分类，同一事务 |
| POST | `/login`    | `username, password`                        | 成功后写入 Session                         |
| POST | `/logout`   | —                                           | 销毁 Session                               |
| GET  | `/current`  | —                                           | 前端刷新页面后恢复登录态                   |

## 二、家庭成员 `/api/member`（乙）

| 方法   | 路径     | 参数                                        | 说明                             |
| ------ | -------- | ------------------------------------------- | -------------------------------- |
| GET    | `/list`  | —                                           | 当前家庭全部成员                 |
| GET    | `/{id}`  | —                                           | 成员详情                         |
| POST   | ` `      | `memberName, relation, monthlyBudget`       | 同一家庭不允许重名               |
| PUT    | `/{id}`  | 同上                                        |                                  |
| DELETE | `/{id}`  | —                                           | 名下还有流水时拒绝删除并给出笔数 |

## 三、收支分类 `/api/category`（乙）

| 方法   | 路径     | 参数                                            | 说明                                     |
| ------ | -------- | ----------------------------------------------- | ---------------------------------------- |
| GET    | `/list`  | `type?`（1=收入 2=支出）                        | 平铺列表，带父分类名                     |
| GET    | `/tree`  | `type?`                                         | 树形，供级联选择器用；一次查询内存建树   |
| POST   | ` `      | `categoryName, parentId, type, sortNo?`         | 最多两级；子分类类型必须与父一致         |
| PUT    | `/{id}`  | `categoryName, sortNo?`                         | 只允许改名与排序，类型和父级不可改       |
| DELETE | `/{id}`  | —                                               | 预置分类、有子分类、被流水引用时均拒绝   |

## 四、收支流水 `/api/record`（丙）★核心

| 方法   | 路径       | 参数                    | 说明                                             |
| ------ | ---------- | ----------------------- | ------------------------------------------------ |
| POST   | `/page`    | `RecordQuery`（见下）   | 返回 `{ page: {total, list}, sumIncome, sumExpense }`，合计按全部匹配记录算 |
| GET    | `/{id}`    | —                       | 详情                                             |
| POST   | ` `        | 流水字段                | 录入                                             |
| PUT    | `/{id}`    | 流水字段                | 编辑                                             |
| DELETE | `/{id}`    | —                       | 删除                                             |
| GET    | `/options` | —                       | 历史商家、历史片区、支付方式候选，供录入页联想   |

`RecordQuery` 字段（全部可空，为空即不参与过滤）：

```
type          1=收入 2=支出
memberId      成员
categoryId    分类；传一级分类时自动含其下全部二级分类
startDate     起始日期（含）
endDate       结束日期（含）
keyword       模糊匹配商家与备注
payMethod     支付方式
area          消费片区
isGift        1=人情往来 0=非
minAmount     金额下限
maxAmount     金额上限
pageNum       页码，从 1 开始，默认 1
pageSize      每页条数，默认 10，上限 200
```

流水字段：`type, memberId, categoryId, amount, recordDate, merchant, area, payMethod, isGift, remark`

服务端校验（`RecordService.validate`）：

- 成员、分类必须属于当前家庭
- 分类的收入/支出类型必须与流水类型一致（防止「工资收入」被记成一笔支出）
- 金额 > 0 且 ≤ 99999999.99，统一保留两位小数
- 发生日期不得晚于今天、不得早于 2000-01-01
- 支付方式只能是 现金/微信/支付宝/银行卡/其他
- 备注不超过 255 字

## 五、统计 `/api/stat`（丁）

只做客观汇总，不下结论。

| 方法 | 路径            | 参数                | 说明                                       |
| ---- | --------------- | ------------------- | ------------------------------------------ |
| GET  | `/overview`     | 区间                | 收入、支出、结余、笔数、月均、单笔最大支出、人情往来支出 |
| GET  | `/trend`        | 区间                | 按月收入/支出/结余；缺失月份补 0，但只补到当月 |
| GET  | `/category`     | `type` + 区间       | 一级分类占比（二级归并到父分类）           |
| GET  | `/sub-category` | `parentId` + 区间   | 二级分类构成，饼图钻取用                   |
| GET  | `/member`       | `type` + 区间       | 成员收支对比                               |
| GET  | `/merchant`     | `limit` + 区间      | 商家消费排行                               |
| GET  | `/area`         | 区间                | 消费片区分布                               |
| GET  | `/pay-method`   | `type` + 区间       | 支付方式构成                               |
| GET  | `/budget`       | `ym`（yyyy-MM）     | 成员预算执行，固定按月口径                 |
| GET  | `/dashboard`    | 区间                | 看板聚合，一次返回首页 5 张图的全部数据    |

## 六、智能分析 `/api/analysis`（丁）★评分点

| 方法 | 路径      | 参数 | 说明                                     |
| ---- | --------- | ---- | ---------------------------------------- |
| GET  | `/report` | 区间 | 返回按严重程度排序的分析结论列表         |

每条结论结构：`{ code, level, title, basis, suggestion }`，
`level` 取 `danger / warning / info / good`。

分析规则：

| 编号 | 规则             | 判定逻辑                                                     |
| ---- | ---------------- | ------------------------------------------------------------ |
| A1   | 异常月份归因     | 支出最高月 vs 其余月平均，超 30% 判异常；再钻取到一级分类找出超支主因，并根据该分类在其他月份的水平判断偶发/持续 |
| A2   | 环比分析         | 最近两个有数据月份支出涨跌超 ±20%                            |
| A3   | 结余健康度       | 结余率 =（收入−支出）/收入；为负判入不敷出，< 10% 判储蓄空间不足 |
| A4   | 成员预算预警     | 使用率 > 100% 判超支，≥ 80% 判接近上限                       |
| A5   | 支出结构集中度   | 占比最高的一级分类 > 50% 判结构单一                          |
| A6   | 商家消费集中度   | 单一商家占已填写商家消费 ≥ 20%                               |
| A7   | 片区消费分布     | 单一片区占比 ≥ 40%                                           |
| A8   | 人情往来专项     | 人情往来支出占总支出 ≥ 10%                                   |

阈值定义在 `AnalysisService` 的常量中，可按家庭实际情况调整。

## 七、资产与贷款 `/api/asset`（戊，拓展）

| 方法   | 路径             | 说明                                             |
| ------ | ---------------- | ------------------------------------------------ |
| GET    | `/summary`       | 资产合计、贷款剩余本金、净资产、月供合计、资产构成 |
| GET    | `/list`          | 资产列表                                         |
| POST   | ` `              | 新增资产                                         |
| PUT    | `/{id}`          | 编辑资产                                         |
| DELETE | `/{id}`          | 删除资产                                         |
| GET    | `/loan/list`     | 贷款列表，附带剩余期数、剩余本金、还款进度       |
| POST   | `/loan`          | 新增贷款                                         |
| PUT    | `/loan/{id}`     | 编辑贷款                                         |
| DELETE | `/loan/{id}`     | 删除贷款                                         |

净资产 = 资产合计 − 贷款剩余本金；
剩余本金按「剩余期数 × 月供」估算，未拆分等额本息的利息部分。
